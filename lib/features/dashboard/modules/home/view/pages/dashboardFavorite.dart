import 'package:flutter/material.dart';
class dashboardFavorite extends StatelessWidget {
  const dashboardFavorite({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("favorite"),
      ),
    );
  }
}
